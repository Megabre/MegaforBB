{% extends 'base.html.twig' %}

{% block content %}
{% set mode = mode ?? 'forgot' %}
{% set error = error ?? '' %}
{% set success = success ?? '' %}
{% set token = token ?? '' %}

<div class="min-h-[60vh] flex items-center justify-center">
  <div class="w-full max-w-3xl bg-white ring-1 ring-gray-200 shadow-sm rounded-lg overflow-hidden grid grid-cols-1 md:grid-cols-2">
    <div class="bg-[#105289] text-white p-6 flex flex-col justify-between">
      <div>
        <div class="text-lg font-semibold">{{ core__('common.site_name') }}</div>
        <div class="text-sm text-gray-200 mt-1">{% if mode == 'forgot' %}Åifrenizi mi unuttunuz? E-posta adresinizi girin.{% else %}Yeni ÅŸifrenizi belirleyin.{% endif %}</div>
        <ul class="mt-4 space-y-2 text-sm">
          <li class="flex items-center gap-2"><i class="fa-solid fa-circle-check text-blue-300"></i>GÃ¼venli ÅŸifre sÄ±fÄ±rlama</li>
          <li class="flex items-center gap-2"><i class="fa-solid fa-circle-check text-blue-300"></i>E-posta ile doÄŸrulama</li>
          <li class="flex items-center gap-2"><i class="fa-solid fa-circle-check text-blue-300"></i>60 dakika geÃ§erlilik</li>
        </ul>
      </div>
      <div class="text-xs text-gray-300">Â© {{ "now"|date("Y") }} {{ core__('common.site_name') }}</div>
    </div>
    <div class="p-6 space-y-4">
      {% if mode == 'forgot' %}
      <div class="text-lg font-semibold text-[#105289]">Åifremi Unuttum</div>
      <p class="text-sm text-gray-600">KayÄ±tlÄ± e-posta adresinizi girin. Size ÅŸifre sÄ±fÄ±rlama linki gÃ¶ndereceÄŸiz.</p>
      {% if error %}
      <div class="p-3 rounded-md bg-red-50 border border-red-200 text-red-700 text-sm">{{ core_e(error) }}</div>
      {% endif %}
      {% if success %}
      <div class="p-3 rounded-md bg-green-50 border border-green-200 text-green-700 text-sm">{{ core_e(success) }}</div>
      {% endif %}
      <form method="post" action="{{ core_url('forgot-password') }}">
        {{ core_csrf_field('forgot_password')|raw }}
        <div class="mb-4">
          <label class="block text-sm font-medium text-[#105289] mb-1">E-posta</label>
          <input name="email" type="email" class="w-full border border-gray-300 rounded-md px-3 py-2 outline-none focus:ring-2 focus:ring-blue-200" placeholder="kullanici@ornek.com" required>
        </div>
        <button type="submit" class="w-full text-white bg-[#105289] hover:bg-[#234a73] rounded-md px-4 py-2">Åifre SÄ±fÄ±rlama Linki GÃ¶nder</button>
      </form>
      {% else %}
      <div class="text-lg font-semibold text-[#105289]">Yeni Åifre Belirle</div>
      <p class="text-sm text-gray-600">Yeni ÅŸifrenizi girin ve tekrar yazÄ±n.</p>
      {% if error %}
      <div class="p-3 rounded-md bg-red-50 border border-red-200 text-red-700 text-sm">{{ core_e(error) }}</div>
      {% endif %}
      <form method="post" action="{{ core_url('reset-password') }}">
        {{ core_csrf_field('reset_password')|raw }}
        <input type="hidden" name="token" value="{{ core_e(token) }}">
        <div class="mb-4">
          <label class="block text-sm font-medium text-[#105289] mb-1">Yeni Åifre</label>
          <input name="password" type="password" class="w-full border border-gray-300 rounded-md px-3 py-2 outline-none focus:ring-2 focus:ring-blue-200" placeholder="â€¢â€¢â€¢â€¢â€¢â€¢â€¢â€¢" required minlength="6">
        </div>
        <div class="mb-4">
          <label class="block text-sm font-medium text-[#105289] mb-1">Åifre Tekrar</label>
          <input name="password_confirm" type="password" class="w-full border border-gray-300 rounded-md px-3 py-2 outline-none focus:ring-2 focus:ring-blue-200" placeholder="â€¢â€¢â€¢â€¢â€¢â€¢â€¢â€¢" required minlength="6">
        </div>
        <button type="submit" class="w-full text-white bg-[#105289] hover:bg-[#234a73] rounded-md px-4 py-2">Åifreyi GÃ¼ncelle</button>
      </form>
      {% endif %}
      <div class="text-sm text-gray-600 text-center"><a href="{{ core_url('login') }}" class="text-blue-600 hover:underline">{{ core__('common.login') }}</a></div>
      <div class="text-center"><a href="{{ core_url('') }}" class="text-sm text-gray-600 hover:underline">{{ core__('common.home') }}</a></div>
    </div>
  </div>
</div>
{% endblock %}
